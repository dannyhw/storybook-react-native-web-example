import {ComponentMeta} from '@storybook/react';

import {Box} from './Box';

export default {
  title: 'libraries/react-native-reanimated/Box',
  component: Box,
} as ComponentMeta<typeof Box>;

export const Basic = {
  args: {},
};
